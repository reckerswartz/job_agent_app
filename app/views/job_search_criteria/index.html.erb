<% content_for(:page_title, "Search Criteria") %>

<div class="page-header d-flex justify-content-between align-items-start mb-4">
  <div>
    <h1 class="page-header__title">Search Criteria</h1>
    <p class="page-header__subtitle">Define what kinds of jobs to search for</p>
  </div>
  <%= link_to "Add Criteria", new_job_search_criterium_path, class: "btn btn-primary" %>
</div>

<% if @criteria.any? %>
  <div class="row g-3">
    <% @criteria.each do |c| %>
      <div class="col-md-6">
        <%= render "job_search_criteria/criteria_card", criteria: c %>
      </div>
    <% end %>
  </div>
<% else %>
  <%= render "shared/empty_state", icon: "&#x1F3AF;", title: "No search criteria yet",
      message: "Define keywords, location, and preferences to target the right jobs.",
      cta_text: "Create Your First Criteria", cta_path: new_job_search_criterium_path %>
<% end %>
