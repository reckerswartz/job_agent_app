<div class="entry-display d-inline-block me-2 mb-2">
  <span class="badge bg-primary"><%= entry.content["name"] %></span>
  <% if entry.content["level"].present? %>
    <small class="text-muted">(<%= entry.content["level"] %>)</small>
  <% end %>
</div>
